import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  env:{
    NEXT_PUBLIC_SANITY_PROJECT_ID: "1bp0q077",
    NEXT_PUBLIC_SANITY_DATASET: "production",
    
  }
};

export default nextConfig;
